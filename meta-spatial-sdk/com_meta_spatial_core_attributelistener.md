# AttributeListener Type Alias

Callback method that is run when the defined attribute is changed

## Signature

```kotlin
typealias AttributeListener = (entity: Entity, dataModel: DataModel, attribute: Int, packet: Any?) -> Unit
```